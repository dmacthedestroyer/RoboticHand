package deprecated;


/**
 * The listener interface for receiving joint events.  A class interested in 
 * processing a joint event implements this interface and the object created 
 * with that class is registered with a component, using the component's 
 * addJointChangeListener method. When the joint event occurs, that object's 
 * jointChanged method is invoked.
 * 
 * @author Daniel McDonald
 * 
 * @see JointEvent
 *
 */
public interface JointChangeListener{
	
	/**
	 * Invoked when a joint event occurs.
	 * @param je {@see JointEvent}
	 */
	public void jointChanged(JointEvent je);
}
